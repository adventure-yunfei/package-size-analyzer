{
  "name": "package-size-analyzer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel cli.js SizeTree.js --out-dir build",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": "./build/cli.js",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^0.30.0",
    "webpack": "^1.13.1",
    "yargs": "^4.7.1"
  },
  "devDependencies": {
    "babel": "^5.8.38"
  }
}
